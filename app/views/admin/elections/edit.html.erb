<% content_for :title, 'Election: Configure' %>

<div class="container-fluid">
  <div class="row">
    <div class="col-sm-10 col-sm-offset-1 main">
      <%= render partial: 'shared/admin_toolbar', locals: {items: [
        @election,
        'Configure'
      ]} %>

      <h2>Configure</h2>

      <%= render partial: 'shared/election_form', locals: { is_creating: false } %>
    </div>
  </div>
</div>
