<% content_for :title, 'Location: Edit' %>

<div class="container-fluid">
  <div class="row">
    <div class="col-sm-10 col-sm-offset-1 main">

      <%= render partial: 'shared/admin_toolbar', locals: {items: [
        @election,
        ['Locations', admin_election_locations_path(@election)],
        @location.name
      ]} %>

      <h2>Edit Location</h2>
      <%= render partial: 'shared/location_form' %>

    </div>
  </div>
</div>
