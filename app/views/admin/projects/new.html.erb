<% content_for :title, 'Project: New' %>

<div class="container-fluid">
  <div class="row">
    <div class="col-sm-10 col-sm-offset-1 main">

      <%= render partial: 'shared/admin_toolbar', locals: {items: [
        @election,
        ['Projects', admin_election_projects_path(@election)],
        'New'
      ]} %>

      <h2>New Project</h2>
      <%= render partial: 'shared/project_form', locals: { is_creating: true } %>

    </div>
  </div>
</div>
